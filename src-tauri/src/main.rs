// Prevents an additional console window on Windows in release builds.
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

mod commands;
mod db;
mod tray;

fn main() {
    let database = db::init_db().expect("no se pudo inicializar la base de datos local de KRYPTOS");

    tauri::Builder::default()
        .plugin(tauri_plugin_shell::init())
        .plugin(tauri_plugin_fs::init())
        .plugin(tauri_plugin_dialog::init())
        .plugin(tauri_plugin_os::init())
        .plugin(tauri_plugin_autostart::init(tauri_plugin_autostart::MacosLauncher::LaunchAgent, None))
        .plugin(tauri_plugin_notification::init())
        .setup(|app| {
            tray::setup_tray(app)?;
            Ok(())
        })
        .manage(commands::terminal::TerminalManager::default())
        .manage(commands::ssh::SshManager::default())
        .manage(commands::file_watch::FileWatchManager::default())
        .manage(commands::chat::ChatManager::default())
        .manage(commands::sentinel::SentinelManager::default())
        .manage(commands::honeytoken::HoneytokenManager::default())
        .manage(commands::database::DatabaseManager::default())
        .manage(database)
        .invoke_handler(tauri::generate_handler![
            commands::system::get_system_snapshot,
            commands::system::list_processes,
            commands::system::kill_process,
            commands::system::get_network_info,
            commands::network_tools::run_ping,
            commands::network_tools::run_traceroute,
            commands::network_tools::run_netstat,
            commands::network_tools::run_ipconfig,
            commands::network_tools::run_port_check,
            commands::network_tools::run_ping_diagnostic,
            commands::terminal::list_available_shells,
            commands::terminal::create_terminal_session,
            commands::terminal::write_to_terminal,
            commands::terminal::resize_terminal,
            commands::terminal::close_terminal_session,
            commands::security::hash_file,
            commands::security::hash_directory,
            commands::security::save_baseline,
            commands::security::list_baselines,
            commands::security::delete_baseline,
            commands::security::compare_baseline,
            commands::security::check_tls_certificate,
            commands::security::search_cve,
            commands::network_scan::run_advanced_scan,
            commands::security_logs::read_security_events,
            commands::firewall::list_firewall_rules,
            commands::firewall::add_firewall_rule,
            commands::firewall::delete_firewall_rule,
            commands::apps::list_applications,
            commands::apps::add_application,
            commands::apps::update_application,
            commands::apps::delete_application,
            commands::apps::get_app_icon_data_url,
            commands::apps::launch_application,
            commands::apps::list_installed_applications,
            commands::audit::list_audit_log,
            commands::audit::clear_audit_log,
            commands::explorer::list_directory,
            commands::explorer::get_home_directory,
            commands::explorer::list_drives,
            commands::explorer::create_directory,
            commands::explorer::create_empty_file,
            commands::explorer::rename_path,
            commands::explorer::delete_path,
            commands::explorer::copy_path,
            commands::explorer::move_path,
            commands::explorer::search_directory,
            commands::explorer::list_favorites,
            commands::explorer::add_favorite,
            commands::explorer::remove_favorite,
            commands::explorer::read_file_text,
            commands::explorer::write_file_text,
            commands::elevation::is_elevated,
            commands::elevation::relaunch_elevated,
            commands::services::list_services,
            commands::services::start_service,
            commands::services::stop_service,
            commands::services::restart_service,
            commands::network_details::list_active_connections,
            commands::network_details::get_network_config,
            commands::network_details::list_arp_table,
            commands::settings::get_setting,
            commands::settings::set_setting,
            commands::settings::get_all_settings,
            commands::settings::get_data_directory,
            commands::settings::open_in_file_manager,
            commands::recon::lookup_dns,
            commands::recon::lookup_whois,
            commands::recon::check_security_headers,
            commands::password_tools::generate_password,
            commands::password_tools::check_password_strength,
            commands::password_tools::check_pwned_range,
            commands::users::list_users,
            commands::logs::read_system_logs,
            commands::git::get_repo_info,
            commands::git::get_repo_status,
            commands::git::get_commit_log,
            commands::git::list_branches,
            commands::git::checkout_branch,
            commands::git::get_file_diff,
            commands::git::stage_file,
            commands::git::unstage_file,
            commands::git::commit_changes,
            commands::git::is_git_repository,
            commands::git::push_to_remote,
            commands::git::fetch_from_remote,
            commands::git::pull_from_remote,
            commands::docker::is_docker_available,
            commands::docker::list_containers,
            commands::docker::start_container,
            commands::docker::stop_container,
            commands::docker::restart_container,
            commands::docker::remove_container,
            commands::docker::get_container_logs,
            commands::docker::list_images,
            commands::docker::remove_image,
            commands::docker::pull_lab_image,
            commands::docker::launch_lab_container,
            commands::wireless::list_wifi_networks,
            commands::wireless::list_bluetooth_devices,
            commands::wireless::list_usb_devices,
            commands::binary_analysis::analyze_binary,
            commands::disk_usage::get_directory_sizes,
            commands::disk_usage::find_duplicate_files,
            commands::scripts::run_script,
            commands::ssh::list_ssh_profiles,
            commands::ssh::add_ssh_profile,
            commands::ssh::delete_ssh_profile,
            commands::ssh::create_ssh_session,
            commands::ssh::write_to_ssh_session,
            commands::ssh::resize_ssh_session,
            commands::ssh::close_ssh_session,
            commands::sftp::sftp_list_directory,
            commands::sftp::sftp_create_directory,
            commands::sftp::sftp_delete_file,
            commands::sftp::sftp_download_file,
            commands::sftp::sftp_upload_file,
            commands::persistence::list_scheduled_tasks,
            commands::audit::export_audit_log,
            commands::file_watch::start_file_watch,
            commands::file_watch::stop_file_watch,
            commands::file_watch::list_active_watches,
            commands::file_crypto::encrypt_file,
            commands::file_crypto::decrypt_file,
            commands::chat::chat_start_server,
            commands::chat::chat_connect,
            commands::chat::chat_send_message,
            commands::chat::chat_disconnect,
            commands::chat::chat_is_active,
            commands::chat::chat_broadcast_status,
            commands::baseline::run_security_baseline,
            commands::window::close_splashscreen,
            commands::panic::panic_lock_session,
            commands::panic::panic_set_network,
            commands::winget::is_winget_available,
            commands::winget::search_winget_packages,
            commands::winget::install_winget_package,
            commands::sentinel::sentinel_start,
            commands::sentinel::sentinel_stop,
            commands::sentinel::sentinel_scan_now,
            commands::sentinel::sentinel_status,
            commands::sentinel::sentinel_list_events,
            commands::sentinel::sentinel_list_alerts,
            commands::sentinel::sentinel_acknowledge_alert,
            commands::sentinel::sentinel_acknowledge_all,
            commands::sentinel::sentinel_reset_reference,
            commands::sentinel::sentinel_clear_history,
            commands::sentinel::sentinel_export,
            commands::honeytoken::honeytoken_deploy,
            commands::honeytoken::honeytoken_list,
            commands::honeytoken::honeytoken_rearm_all,
            commands::honeytoken::honeytoken_remove,
            commands::dossier::investigate_process,
            commands::sentinel::sentinel_time_bounds,
            commands::sentinel::sentinel_state_at,
            commands::database::list_db_connections,
            commands::database::add_db_connection,
            commands::database::delete_db_connection,
            commands::database::db_test_connection,
            commands::database::db_connect,
            commands::database::db_disconnect,
            commands::database::db_list_tables,
            commands::database::db_list_columns,
            commands::database::db_run_query,
        ])
        .run(tauri::generate_context!())
        .expect("error al ejecutar la aplicacion KRYPTOS");
}

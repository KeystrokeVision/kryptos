========================================================================
 KRYPTOS — Resultados de las pruebas de cada pipeline DevOps
========================================================================
Fecha de ejecución: 2026-08-12
Entorno de prueba: local (Windows, Node v24.14.1, npm 11.11.0)
Rama: devops/dora-metrics (sin push a GitHub todavía)

IMPORTANTE — qué significa "resultado de la prueba" acá:
Ningún pipeline externo (GitHub Actions real, Azure DevOps, Jenkins,
CircleCI, Harness) corrió todavía porque:
  - no se hizo push de la rama a GitHub (GitHub Actions no se disparó), y
  - las otras 4 plataformas no tienen cuenta/proyecto conectado.
Por eso, lo que se probó fue LO QUE CADA PIPELINE EJECUTA POR DENTRO:
el comando de build real (idéntico en los 5), la validación de sintaxis
de cada archivo de configuración, y el script de métricas que todos
comparten. Son pruebas reales, ejecutadas de verdad, no simuladas
"a mano" sobre el papel.

------------------------------------------------------------------------
PRUEBA 1 — Build real (paso común a los 5 pipelines)
------------------------------------------------------------------------
Comando ejecutado: npm run build   (== "tsc -b && vite build")
Este es el comando exacto que corren:
  - .github/workflows/ci.yml
  - azure-pipelines.yml (stage Build)
  - Jenkinsfile (stage "Build & Typecheck")
  - .circleci/config.yml (step "Typecheck + Build")

RESULTADO: ✅ ÉXITO
  - Exit code: 0
  - Duración: 73 segundos (1m 13s)
  - Typecheck (tsc -b): sin errores
  - Vite build: 3335 módulos transformados, bundle generado en dist/
  - Advertencia (no error): algunos chunks superan 2500 kB tras
    minificar (vendor-monaco-*.js pesa ~3.83 MB). No rompe el build,
    pero es una oportunidad de mejora a futuro (code-splitting).

Conclusión: Build Success Rate de esta corrida = 100% (1/1).
Esto es exactamente lo que Azure DevOps ("tasa de fallos en builds"),
Jenkins/CircleCI ("Build Success Rate") y GitHub Actions medirían si
corrieran ahora mismo.

------------------------------------------------------------------------
PRUEBA 2 — Validación de sintaxis de los 5 archivos YAML de pipeline
------------------------------------------------------------------------
Herramienta: js-yaml (parser YAML real, instalado temporalmente solo
para esta prueba, sin modificar package.json/package-lock.json).

Archivo                                    | Resultado
--------------------------------------------|------------
.github/workflows/ci.yml                    | ✅ YAML OK
.github/workflows/dora-metrics.yml          | ✅ YAML OK
azure-pipelines.yml                         | ✅ YAML OK
.circleci/config.yml                        | ✅ YAML OK
.harness/kryptos-pipeline.yaml              | ✅ YAML OK

Conclusión: los 5 archivos son sintácticamente válidos y quedarían
aceptados por sus respectivas plataformas al importarlos (sujeto a que
existan los recursos referenciados: agentes, conectores, servicios).

------------------------------------------------------------------------
PRUEBA 3 — Jenkinsfile (chequeo de balance de sintaxis)
------------------------------------------------------------------------
No hay un servidor Jenkins disponible en este entorno para correrlo de
verdad, así que se hizo un chequeo de balance de llaves `{ }` (typo más
común que rompe un pipeline declarativo de Jenkins).

RESULTADO: ✅ Llaves abiertas: 17 | Llaves cerradas: 17 | Balanceado: sí

Conclusión: no hay errores obvios de estructura. La validación
definitiva requiere correrlo en un Jenkins real (Jenkins tiene un
linter propio: "Replay" / "Pipeline Syntax" / `jenkins-cli declarative-linter`).

------------------------------------------------------------------------
PRUEBA 4 — scripts/dora-metrics.mjs (motor de métricas compartido)
------------------------------------------------------------------------
Se probaron los 3 comandos contra el repo real:

4.1) record-build success <sha-real-del-HEAD> "local-test-run"
     RESULTADO: ✅ evento "success" registrado correctamente en
     metrics/failures.jsonl

4.2) record-deploy "test-v0.0.1-local" <sha-real-del-HEAD>
     RESULTADO: ✅ evento "deployment" registrado; como no hay tags
     previos en el repo, calculó el lead time contra el primer commit
     del historial -> 7.48 horas.

4.3) Simulación de falla + recuperación (para validar Change Failure
     Rate y MTTR, que necesitan al menos una falla para tener datos):
       record-build failure fake111 https://example.local/run-1
       record-build success fake222 https://example.local/run-2
     RESULTADO: ✅ el script detectó la recuperación automáticamente
     ("Recuperación detectada: MTTR = 0h") y el reporte agregado dio:

       Change Failure Rate: 50.0% (1/2 builds en main)
       MTTR (Mean Time To Recovery): 0.00h

     (0h es correcto: la falla y el éxito se registraron en el mismo
     segundo porque fue una prueba manual instantánea; en producción,
     con builds reales separadas en el tiempo, el MTTR reflejará el
     tiempo real transcurrido).

4.4) npm run dora:report (reporte agregado final de la prueba)

       == Reporte de métricas DORA — Kryptos ==
       Deployment Frequency: 1 despliegue(s) totales, 1 en los
         últimos 30 días (~0.23/semana)
       Lead Time for Changes (promedio): 7.48h
       Change Failure Rate: 50.0% (1/2 builds en main)
       MTTR (Mean Time To Recovery): 0.00h

RESULTADO GENERAL: ✅ las 4 métricas DORA se calculan correctamente a
partir de eventos reales.

NOTA: después de esta prueba, metrics/deployments.jsonl y
metrics/failures.jsonl se dejaron vacíos otra vez (git status limpio)
para que el historial de métricas real empiece desde cero cuando el
proyecto se use en producción, sin datos de prueba mezclados.

------------------------------------------------------------------------
PRUEBA 5 — Azure DevOps / CircleCI / Harness (ejecución real)
------------------------------------------------------------------------
RESULTADO: ⏸️ NO EJECUTADO — requiere cuenta/proyecto conectado en cada
plataforma, que no existe en este entorno. Lo que SÍ se validó (sintaxis
+ el build que ejecutan por dentro) está cubierto en las Pruebas 1 y 2.

Para obtener un resultado real de estas 3 plataformas hace falta que:
  - Azure DevOps: importes el repo a un proyecto de Azure DevOps.
  - CircleCI: conectes el repo en circleci.com (login con GitHub).
  - Harness: crees cuenta/proyecto y reemplaces los placeholders
    (<TU_ORG_HARNESS>, <TU_PROYECTO_HARNESS>, <TU_CONECTOR_*>) en
    .harness/kryptos-pipeline.yaml.

------------------------------------------------------------------------
RESUMEN FINAL
------------------------------------------------------------------------
  Prueba                                          | Resultado
  --------------------------------------------------|------------
  Build real (npm run build)                        | ✅ ÉXITO (73s)
  Sintaxis YAML (5 archivos)                         | ✅ 5/5 OK
  Sintaxis Jenkinsfile (balance de llaves)           | ✅ OK
  Script de métricas — record-deploy                | ✅ OK
  Script de métricas — record-build (éxito)          | ✅ OK
  Script de métricas — falla + recuperación (MTTR)   | ✅ OK
  Reporte agregado (npm run dora:report)             | ✅ OK
  Ejecución real en Azure DevOps / CircleCI / Harness | ⏸️ pendiente
                                                        (requiere cuentas)
  Ejecución real en GitHub Actions                   | ⏸️ pendiente
                                                        (requiere push)

Todo lo que se puede probar sin depender de servicios externos con
cuenta propia fue probado y pasó. Lo que falta (correr los pipelines
"en vivo" en cada plataforma) requiere que hagas push de la rama a
GitHub y/o me des acceso/credenciales a Azure DevOps, Jenkins, CircleCI
y Harness.
========================================================================
Fin del reporte.
========================================================================
